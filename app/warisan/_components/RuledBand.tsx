/**
 * The stack of thin parallel rules the reference sets between the hero and the
 * page proper. Easy to miss in a screenshot, but it is what makes the seam
 * between the dark hero and the cream ground feel deliberate instead of abrupt.
 */
export function RuledBand() {
  return <div className="ruled-band w-full" aria-hidden />;
}
